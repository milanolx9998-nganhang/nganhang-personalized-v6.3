APP_PROFILE=local-lan
DATABASE_PROVIDER=postgres
STORAGE_PROVIDER=local
NODE_ENV=production
DOMAIN=bank.school.internal
DB_PASSWORD=
JWT_SECRET=
# Generate independent values locally. Never copy real .env between hosts.
