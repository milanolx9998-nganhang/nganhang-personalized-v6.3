import test from 'node:test';import assert from 'node:assert/strict';
import {access,position,org} from './fixtures.js';import {decide} from '../../src/services/access/policy.js';import {PRESETS} from '../../src/services/access/catalog.js';
test('V652 exploit: giáo viên khối 8 không ghi khối 9, nhưng giữ khối 8',()=>{const a=access([position('SUBJECT_TEACHER',{subject_ids:[5],class_ids:[103],school_year_id:1})]);assert.equal(decide(a,'content.write',{subjectId:5,grade:9}).allowed,false);assert.equal(decide(a,'content.write',{subjectId:5,grade:8}).allowed,true);assert.equal(decide(a,'matrix.create',{subjectId:5,grade:9}).allowed,false);});
test('V652 exploit: môn không có lớp không mở nội dung toàn trường',()=>{const a=access([position('SUBJECT_TEACHER',{subject_ids:[5],school_year_id:1})]);assert.equal(decide(a,'content.write',{subjectId:5,grade:8}).allowed,false);});
test('V652 exploit: viewer/BGH giám sát không có đáp án mặc định',()=>{for(const type of ['VIEWER','BOARD']){const a=access([position(type,{class_ids:[101]})]);assert.equal(decide(a,'content.view_answer',{subjectId:5,grade:7}).allowed,false,type);}});
test('V652 exploit: preset không tự cấp export/source/lock hoặc quản trị',()=>{for(const type of ['SUBJECT_TEACHER','DEPT_LEADER','BOARD','GRADE_LEADER','HOMEROOM'])for(const key of ['content.export','content.export_answers','content.download_source','analytics.export','matrix.lock','staff.manage','system.config'])assert.equal(PRESETS[type].includes(key),false,type+':'+key);});
test('V652 exploit: tài khoản khóa không có quyền hiệu lực, kể cả admin',()=>{for(const role of ['teacher','admin']){const a=access([position('BOARD',{}, {scope_type:'WHOLE_SCHOOL'})],[],{user:{id:2,role,is_active:false}});assert.equal(decide(a,'content.read',{subjectId:5,grade:7}).allowed,false);}});
