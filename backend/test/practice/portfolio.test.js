import test from 'node:test';import assert from 'node:assert/strict';
import {historyQuery,hasResponse,reviewCounts,learningSignals} from '../../src/services/practice/portfolioRules.js';
test('Hồ sơ: bộ lọc ngày/phân trang chặn đầu vào sai',()=>{assert.equal(historyQuery.parse({}).limit,20);for(const q of [{limit:51},{offset:-1},{from:'2026-02-30'},{from:'2026-09-02',to:'2026-09-01'},{min_percentage:60,max_percentage:50}])assert.throws(()=>historyQuery.parse(q));});
test('Hồ sơ: đếm riêng đúng, partial, sai, bỏ qua, tự luận, chưa chắc',()=>{const item=(score,extra={})=>({question:{auto_gradable:true},response:'A',result:{score},...extra});assert.deepEqual(reviewCounts([item(1),item(.5),item(0),item(0,{skipped:true,response:null}),item(null,{question:{auto_gradable:false},uncertain:true})]),{correct:1,partial:1,incorrect:1,skipped:1,uncertain:1,essay:1,pending:0,answered:4});assert(hasResponse(false));assert(hasResponse(0));assert(!hasResponse({value:'',unit:'m'}));});
test('Hồ sơ: độ tin cậy thấp không bị gắn nhãn yếu',()=>{const signals=learningSignals([{mastery_score:30,confidence:'LOW',trend:'DOWN'},{mastery_score:60,confidence:'MEDIUM',trend:'STABLE'},{mastery_score:90,confidence:'HIGH',trend:'UP'}]);assert.equal(signals.focus.length,1);assert.equal(signals.strong.length,1);assert.equal(signals.low_confidence,1);});
