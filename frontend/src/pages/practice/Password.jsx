import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../../api/client.js';import {useAuth} from '../../hooks/useAuth.js';import {ErrorBox} from './shared.jsx';
export default function Password(){const nav=useNavigate(),{logout}=useAuth();const[old,setOld]=useState(''),[next,setNext]=useState(''),[error,setError]=useState('');return <section className="practice-page"><h1>Đổi mật khẩu</h1><p>Đặt mật khẩu riêng trước khi bắt đầu sử dụng.</p><ErrorBox error={error}/><form onSubmit={async e=>{e.preventDefault();try{await api.post('/api/auth/change-password',{old_password:old,new_password:next});logout();nav('/login');}catch(e){setError(e.message);}}}><label>Mật khẩu hiện tại<input type="password" autoComplete="current-password" required value={old} onChange={e=>setOld(e.target.value)}/></label><label>Mật khẩu mới<input type="password" autoComplete="new-password" minLength={12} required value={next} onChange={e=>setNext(e.target.value)}/></label><button className="btn primary">Lưu và đăng nhập lại</button></form></section>;}
